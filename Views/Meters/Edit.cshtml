@using QL_HethongDiennuoc.Models.DTOs
@model MeterDto

@{
    ViewData["Title"] = "Chỉnh sửa Công tơ";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-pencil-square"></i> Chỉnh sửa Công tơ</h2>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Quay lại
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0"><i class="bi bi-speedometer"></i> @Model.MeterNumber</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label class="form-label fw-bold">Khách hàng</label>
                            <input type="text" class="form-control" value="@Model.CustomerName" disabled />
                            <small class="text-muted">Không thể thay đổi khách hàng của công tơ</small>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Số Công tơ</label>
                            <input name="MeterNumber" class="form-control" value="@Model.MeterNumber" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Loại</label>
                            <input type="text" class="form-control" value="@(Model.Type == "Electric" ? "⚡ Điện" : "💧 Nước")" disabled />
                            <small class="text-muted">Không thể thay đổi loại công tơ</small>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Vị trí lắp đặt</label>
                            <input name="Location" class="form-control" value="@Model.Location" placeholder="VD: Tầng 1, Phòng 101" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Trạng thái</label>
                            <div class="form-check form-switch">
                                <input type="checkbox" name="IsActive" value="true" class="form-check-input" id="isActiveSwitch" @(Model.IsActive ? "checked" : "") />
                                <label class="form-check-label" for="isActiveSwitch">
                                    @(Model.IsActive ? "Đang hoạt động" : "Ngừng hoạt động")
                                </label>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Ngày lắp đặt</label>
                            <input type="text" class="form-control" value="@Model.InstallDate.ToString("dd/MM/yyyy")" disabled />
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-warning">
                                <i class="bi bi-check-lg"></i> Cập nhật
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-x-lg"></i> Hủy
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
