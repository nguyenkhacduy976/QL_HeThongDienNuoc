@using QL_HethongDiennuoc.Models.DTOs
@model CreateMeterDto

@{
    ViewData["Title"] = "Thêm Công tơ mới";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-plus-circle"></i> Thêm Công tơ mới</h2>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Quay lại
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="bi bi-speedometer"></i> Thông tin Công tơ</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label class="form-label fw-bold">Khách hàng <span class="text-danger">*</span></label>
                            <select asp-for="CustomerId" asp-items="@ViewBag.Customers" class="form-select">
                                <option value="">-- Chọn khách hàng --</option>
                            </select>
                            <span asp-validation-for="CustomerId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="MeterNumber" class="form-label fw-bold">Số Công tơ <span class="text-danger">*</span></label>
                            <input asp-for="MeterNumber" class="form-control" placeholder="VD: DH-001234" />
                            <span asp-validation-for="MeterNumber" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Type" class="form-label fw-bold">Loại <span class="text-danger">*</span></label>
                            <select asp-for="Type" class="form-select">
                                <option value="">-- Chọn loại --</option>
                                <option value="1">⚡ Điện</option>
                                <option value="2">💧 Nước</option>
                            </select>
                            <span asp-validation-for="Type" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Location" class="form-label fw-bold">Vị trí lắp đặt</label>
                            <input asp-for="Location" class="form-control" placeholder="VD: Tầng 1, Phòng 101" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="InstallDate" class="form-label fw-bold">Ngày lắp đặt</label>
                            <input asp-for="InstallDate" type="date" class="form-control" value="@DateTime.Now.ToString("yyyy-MM-dd")" />
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-lg"></i> Thêm Công tơ
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-x-lg"></i> Hủy
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
